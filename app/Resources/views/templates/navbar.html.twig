<nav class="navbar navbar-default navbar-static-top" data-spy="affix" data-offset-top="197">
    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse"
                    data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand brand-decor" href="{{ path('products_all') }}">BlueFire</a>
        </div>
        <div class="collapse navbar-collapse"
             id="bs-example-navbar-collapse-1">
            {% if is_granted('ROLE_USER') %}
                <ul class="nav navbar-nav navbar-right">
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true"
                           aria-expanded="false"> My Profile <span class="caret"></span></a>
                        <ul class="dropdown-menu">
                            <li><a href="{{ path('logout') }}">Logout</a></li>
                            {% if is_granted('ROLE_MODERATOR') %}
                                <li role="separator" class="divider"></li>
                                <li><a href="{{ path('product_crud') }}">Product CRUD</a></li>
                            {% endif %}
                            {% if is_granted('ROLE_ADMIN') %}
                                <li role="separator" class="divider"></li>
                                <li><a href="{{ path('user_crud') }}">User CRUD</a></li>
                            {% endif %}
                        </ul>
                    </li>
                </ul>
            {% endif %}
        </div>
    </div>
</nav>